#!/usr/bin/env bash
set -euo pipefail
if [ "$#" -ne 1 ]; then
  echo "Usage: oc-job-get <job_id>" >&2
  exit 2
fi
job_id="$1"
if ! [[ "$job_id" =~ ^[a-f0-9]{10}$ ]]; then
  echo "job_id must match [a-f0-9]{10}" >&2
  exit 2
fi
exec /root/operator/bin/op job get "$job_id"
